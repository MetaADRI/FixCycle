{
  "name": "fixcycle-api-server",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone API server for Fixcycle PWA — zero dependencies",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
